
  # Finance Agent

  This is a code bundle for Finance Agent. The original project is available at https://www.figma.com/design/aWBCxvKzerXFSUdKcsYzjY/Finance-Agent.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  